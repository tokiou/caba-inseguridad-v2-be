.env
data/raw/*.xlsx
data/processed/*.jsonl
# OSM extracts and their converted XML — offline build inputs, regenerated by
# scripts/osm/download_caba_osm.sh + import_osm_graph.sh.
data/osm/
__pycache__/
# Editor / agent tooling — local, not part of the project
.claude/projects/
.opencode/
opencode.json
.vscode/

# Benchmark build artifacts + noisy per-run server logs (the k6 summaries and
# server-stat snapshots under bench/results/ ARE committed for regression tracking).
bench/bin/
bench/results/**/api.log

# Go test coverage output
*.out
coverage.*
